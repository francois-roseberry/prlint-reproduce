{
  "name": "prlint-reproduce",
  "version": "1.0.0",
  "description": "Just a simple project to reproduce an issue",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@commitlint/cli": "19.2.1",
    "@commitlint/config-conventional": "19.1.0"
  }
}
